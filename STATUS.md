# STATUS.md — Prime F&H

> Actualizar este archivo al final de CADA sesión de Claude Code.
> Marcar `[x]` el paso completado (según `BLUEPRINT.md` §9) y anotar en el registro de sesiones.

**Última actualización:** 2026-08-05
**Stack:** Vite + React (SPA/PWA) + Express + MongoDB Atlas — NO Next.js/Supabase (ver `CLAUDE.md`)
**Deploy:** Backend en Railway (`prime-saas-production.up.railway.app`, auto-deploy desde `main`) · Frontend en Hostinger (`https://app.primefh.cl`, subida manual del build)

> 📘 **El plano completo del proyecto (arquitectura, seguridad, modelo de datos, y los 28 pasos de construcción) vive en `BLUEPRINT.md`.** Este archivo (`STATUS.md`) solo lleva el control de avance contra esos pasos — no duplica el diseño, para que no se desactualice en dos lugares distintos.

---

## 📸 ESTADO REAL (resumen — detalle verificado en `BLUEPRINT.md` §1.2 y Resumen Ejecutivo)

Hay más construido de lo que parecía a simple vista: auth por roles, agenda con la regla de 4 simultáneos, cancelación con regla de 4 horas, disponibilidad por profesional, sistema de planes (a medias), mediciones/ejercicios/dolor con gráficos, PWA funcionando.

Pero una auditoría completa (2026-08-05) encontró **vulnerabilidades graves en producción** (recuperación de contraseña insegura, creación automática de cuentas desde Airtable, fichas médicas accesibles entre pacientes, escalada de roles, entre otras) y features a medio terminar (el descuento de sesiones del sistema de planes nuevo nunca se ejecuta). El detalle completo, verificado línea por línea, está en `BLUEPRINT.md` — no se repite aquí para evitar que este resumen quede desactualizado. **La Fase A del blueprint (pasos 01–07) tapa lo urgente y va primero.**

---

## 📋 CHECKLIST — BLUEPRINT.md §9 (28 pasos en 4 fases)

### FASE A — Hotfixes de seguridad ✅ COMPLETA (7/7)
- [x] Paso 01 — Cerrar recuperación insegura y registro público ✅ 2026-08-05 (`step-01-auth-doors`)
- [x] Paso 02 — Eliminar puerta trasera de Airtable ✅ 2026-08-05 (`step-02-airtable`)
- [x] Paso 03 — Sacar `.env.production` del repo ✅ 2026-08-05 (`step-03-secrets`) — la rotación resultó innecesaria (ver registro)
- [x] Paso 04 — Middleware de pertenencia y cierre de IDORs ✅ 2026-09-06 (`step-04-idor`) — **verificado con 24 pruebas reales contra la base de producción**
- [x] Paso 05 — Anti-escalada de roles y política de contraseñas ✅ 2026-09-06 (`step-05-roles`)
- [x] Paso 06 — Hardening del servidor (helmet, rate limiting, sanitización, validación) ✅ 2026-09-06 (`step-06-hardening`)
- [x] Paso 07 — Cuarentena de Google Calendar + suite de pruebas ✅ 2026-09-06 (`step-07-quarantine`) — **FASE A COMPLETA**

### FASE B — Autenticación definitiva ✅ código completo (7/7) — infraestructura de Mario pendiente (Pasos 09/11)
- [x] Paso 08 — Refresh tokens: modelo y endpoints ✅ 2026-09-07 (`step-08-refresh`)
- [~] Paso 09 — Dominio `api.primefh.cl` + CORS definitivo — código listo, falta que Mario cree el dominio en Railway + CNAME en Hostinger
- [x] Paso 10 — Frontend: token en memoria, fin del PHI en localStorage ✅ 2026-09-07 (`step-10-frontend-auth`) — código en `main`, NO subir el build a Hostinger hasta cerrar el Paso 09
- [~] Paso 11 — Resend: servicio de email con dominio verificado — código en producción, falta que Mario configure la cuenta de Resend
- [~] Paso 12 — Onboarding por invitación (backend) — código listo y probado, **NO subido a producción todavía** (ver nota abajo)
- [~] Paso 13 — Onboarding y reset por email (frontend) — código listo y probado, backend en producción, **frontend NO subido a Hostinger todavía**
- [x] Paso 14 — Limpieza de rutas y guards por rol (frontend) ✅ 2026-09-08 (`step-14-guards`) — **FASE B COMPLETA**, frontend pendiente de subir junto con Pasos 10/12/13

### FASE C — Planes y agenda consolidados
- [~] Paso 15 — Motor de descuento de sesiones — código listo y probado (`cb4fa7a`), **NO subido a producción** (ver nota abajo)
- [~] Paso 16 — Migrar y retirar el modelo `Plan` legacy — código listo y probado, **NO subido a producción**, va junto con el Paso 15
- [~] Paso 17 — Reglas de agenda al 100%: ventana 4h, cron de expiración, recurrente completa — código listo y probado, **NO subido a producción**; falta que Mario configure el cron en Railway (ver nota abajo)
- [~] Paso 18 — Fin de los precios en la app y consolidación de UI de planes — código listo y probado, **NO subido a producción**

### FASE D — Features faltantes
- [~] Paso 19 — Notificaciones (backend) — código listo y probado, **NO subido a producción**
- [~] Paso 20 — Campanita (frontend) — código listo y probado, **NO subido a producción**
- [ ] Paso 21 — Archivos médicos en R2 (backend)
- [ ] Paso 22 — Archivos médicos (UI)
- [~] Paso 23 — Wellness check-in (modelo + paciente) — código listo y probado, **NO subido a producción**
- [~] Paso 24 — Wellness: tendencias para staff/admin — código listo y probado, **NO subido a producción**
- [~] Paso 25 — Historial clínico completo + export CSV — código listo y probado, **NO subido a producción**
- [~] Paso 26 — PWA pulida y headers del frontend — código listo y probado, **NO subido a producción**
- [~] Paso 27 — Salud, monitoreo y respaldos — solo la parte (a) hecha, **(b) y (c) bloqueadas en decisiones/cuentas de Mario** (ver nota abajo)
- [~] Paso 28 — Google Calendar (staff OAuth + .ics pacientes) — código listo y probado, **NO subido a producción** (ver nota abajo)

---

## 🚀 ANTES DE ONBOARDEAR PACIENTES REALES

Checklist completa en `BLUEPRINT.md` §20 (Puertas de aceptación): smoke test de seguridad ejecutable, checklist funcional en iPhone/Android, credenciales rotadas, respaldo probado con una restauración real, datos de prueba eliminados.

---

## 📝 REGISTRO DE SESIONES

| Fecha | Paso / Etapa | Qué se hizo | Modelo | Commit | Notas / pendientes |
|---|---|---|---|---|---|
| 2026-09-09 | Paso 28 (Fase D) — **calendario para todos: staff y pacientes** | **28.A — Google Calendar real para staff (rediseño completo de la integración que estaba en cuarentena desde el Paso 07).** `User.googleTokens` (nuevo, cifrado con AES-256-GCM vía `backend/utils/encryption.js`, `select:false` igual que `password` — y además excluido explícitamente en el `toJSON` del modelo, doble capa). `Appointment.googleEventId` (nuevo). `backend/controllers/googleCalendarController.js` (nuevo, la lógica antes vivía suelta en la ruta) reemplaza los 6 problemas documentados en el código en cuarentena: cliente OAuth por petición (nunca un singleton compartido entre usuarios), `state` firmado con JWT y atado al usuario que inició el flujo (previene que alguien complete el login de Google de otra persona y quede vinculado a la cuenta equivocada — CSRF clásico de OAuth), scope mínimo `calendar.events` (antes pedía acceso total al calendario), y verificación de pertenencia explícita antes de sincronizar (un profesional solo puede sincronizar SUS propias citas — 404, no 403, si intenta con la de otro). Endpoints: `GET /auth-url`, `POST /callback`, `POST /disconnect`, `GET /status`, `POST /sync/:appointmentId` — todos protegidos a `admin`/`professional`. Frontend: card "Google Calendar" nueva en `Configuración` (conectar/desconectar/estado), página de tránsito `/auth/google/callback` que recibe la respuesta de Google y hace el intercambio, y en el Calendario de staff un clic sobre una cita propia la sincroniza (con manejo explícito del caso "todavía no conectaste tu cuenta", con botón de acción al toast). **28.B — descarga `.ics` para pacientes (sin OAuth, RFC 5545).** `backend/services/icsService.js` (nuevo) arma el archivo con `TZID=America/Santiago` (evita hacer matemática de offset/horario de verano a mano — se la delega al cliente de calendario) y el escapado de texto que pide el estándar. `GET /appointments/:id/ics` (con verificación de pertenencia: un paciente solo descarga las suyas). Botón "Agregar a mi calendario" en `Mis Citas` y en el toast de éxito al reservar una cita nueva. **Verificado de punta a punta contra la base real:** con un profesional y un paciente de prueba (creados y borrados en esta sesión), confirmé que `/auth-url` genera la URL y redirige a Google de verdad, que sincronizar una cita ajena da 404, que sincronizar sin haber conectado Google da el mensaje correcto con el botón "Ir a Configuración", y que el `.ics` descargado por curl tiene el formato exacto esperado (`TZID`, escapado, 60 minutos de duración). 17 pruebas nuevas (6 de `.ics`, 11 de Google Calendar con `googleapis` mockeado — sin credenciales reales, no hay forma de probar el intercambio de código contra Google de verdad), 128 pruebas de backend en verde en total, tsc y `npm run build` (frontend) limpios. **De paso, arreglé una prueba vieja rota** (`backend/tests/bookingRules.test.js`): combinaba una fecha calculada con una hora fija `10:00`, y dejó de representar "N horas desde ahora" en cuanto la hora real del día pasó las 10:00 — cosa que pasó durante esta misma sesión, de tanto rato corriendo. | Sonnet 5 | (pendiente) | Va junto con los Pasos 15-27. **Para que esto funcione en producción, Mario necesita, además del push:** (1) crear una app OAuth en Google Cloud Console (tipo "Web application", con `https://app.primefh.cl/auth/google/callback` como redirect URI autorizado) y cargar `GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET` en Railway; (2) generar y cargar `ENCRYPTION_KEY` en Railway (instrucción exacta en `backend/.env.example`) — sin ella, conectar Google Calendar falla con un error claro, el resto de la app sigue funcionando igual. La parte de pacientes (28.B, el `.ics`) no necesita nada de esto — funciona apenas se suba. Esta sesión ya acumula 12 pasos del blueprint sin ningún push a producción — cada vez es más urgente que Mario revise y decida qué desplegar. Quedan sin bloqueos de código: nada nuevo — todo lo que falta (Pasos 21/22, y las partes (b)/(c) del Paso 27) depende de decisiones o cuentas externas de Mario. |
| 2026-09-09 | Paso 27 (Fase D, parcial) | **`GET /api/health` — la única parte de este paso que se podía hacer sin Mario.** Responde `{"status":"ok","db":"connected"}` (200) si Mongo está conectado, o `{"status":"error","db":"disconnected"}` (503) si no — sin autenticación, sin ningún dato sensible, para que un monitor externo lo pueda pegar sin credenciales. Montado antes del rate limiter general para que un chequeo cada 5 minutos nunca compita por ese cupo con tráfico real. Verificado contra el servidor real (200, formato exacto que pide el blueprint) y con 2 pruebas nuevas (111 en total, todas en verde). **Las otras dos partes del paso NO se hicieron — son decisiones/cuentas que le corresponden a Mario, no código:** (b) crear el monitor gratuito en UptimeRobot apuntando a `https://api.primefh.cl/api/health` cada 5 min con alerta a su email — es una cuenta externa, no algo que se pueda automatizar desde acá; (c) respaldos — el blueprint pide elegir entre subir MongoDB Atlas a un plan pago (~US$9/mes, con backups automáticos) o un script gratuito que respalda a Cloudflare R2 — lo segundo depende de la misma cuenta de Cloudflare que están esperando los Pasos 21/22, y lo primero es un gasto que solo Mario puede autorizar. El blueprint marca esto como **obligatorio antes del primer paciente real** — no es algo para posponer indefinidamente. | Sonnet 5 | (pendiente) | Va junto con los Pasos 15-26. **Pendiente de Mario, dos decisiones separadas:** (1) crear el monitor en UptimeRobot (5 minutos, gratis); (2) elegir entre pagar Atlas M2 o esperar a Cloudflare R2 para los respaldos — esto último es genuinamente urgente antes de tener pacientes reales usando el sistema en producción, a diferencia de la mayoría de los demás pendientes de esta sesión. Siguiente candidato de código puro: Paso 28 (calendario — pero el propio blueprint lo marca opcional y es una integración grande con Google OAuth); o esperar las decisiones de Mario para retomar los Pasos 21/22/27(c). |
| 2026-09-09 | Paso 26 (Fase D) | **PWA pulida: íconos reales, limpieza de config muerta, headers de seguridad, página de instalación.** Los 4 íconos del manifest (`icon-192`, `icon-512`, `icon-512-maskable`, `apple-touch-icon`) generados desde el logo cuadrado ya existente (`src/assets/logo-prime-fh.png`, 512×512) con `sips` (herramienta nativa de macOS, sin instalar nada) — antes `public/icons/` estaba vacía y esas URLs daban 404. `vite.config.ts`: `theme_color` pasó de un azul genérico (`#0EA5E9`) al teal de marca (`#3D9AA6`, el mismo que usan los emails desde el Paso 11), `lang: 'es'` agregado, y se eliminó la única regla de `runtimeCaching` que existía — cacheaba `*.supabase.co`, un resto de un scaffold que este proyecto nunca usó (nunca hubo Supabase aquí); de paso confirma que el service worker no cachea `api.primefh.cl` (nunca lo hizo, pero ahora es explícito: cero reglas de runtime caching). **`dev-dist/` sacado del control de versiones** (a `.gitignore` + `git rm --cached`) — era un artefacto autogenerado del modo desarrollo de la PWA que llevaba toda la sesión apareciendo como "modificado" en cada `git status` sin ser parte de ningún cambio real. `index.html`: se sacó `Inter` del link de Google Fonts (confirmé que ya viene autohosteado via `@fontsource/inter` desde `src/main.tsx` — estaba cargándose dos veces) y se agregó `<meta name="theme-color">`; Playfair Display se queda en Google Fonts porque no tiene paquete autohosteado. `public/.htaccess` (ya existía, con la regla de reescritura para rutas de React Router — no se tocó) gana headers de seguridad: HSTS, `X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy`, y una CSP básica. **Ajuste sobre el CSP literal del blueprint:** el ejemplo del blueprint no incluía `style-src`/`font-src` para Google Fonts — aplicado tal cual habría roto la carga de Playfair Display (que sigue viniendo de ahí); se agregó `https://fonts.googleapis.com` a `style-src` y `https://fonts.gstatic.com` como `font-src` nuevo, lo mínimo necesario para no romper algo que hoy funciona. Página nueva `/instalar` (pública, sin login): instrucciones manuales para iPhone/iPad (Safari no dispara instalación programática, solo tiene el flujo de "Compartir → Agregar a pantalla de inicio") y Android/Chrome, más un botón que aparece solo cuando el navegador sí ofrece el evento `beforeinstallprompt` nativo. Verificado con los 3 comandos exactos que pide el blueprint (`ls public/icons \| wc -l` → 4, `grep -c supabase dist/sw.js` → 0, headers presentes en `dist/.htaccess`) más las 4 URLs de íconos respondiendo 200 en el navegador real, el manifest generado con los valores correctos, y `/instalar` renderizando bien en móvil. tsc y `npm run build` limpios; 109 pruebas de backend sin cambios (paso 100% frontend/infra). | Sonnet 5 | (pendiente) | Va junto con los Pasos 15-25. Esta sesión ya acumula 10 pasos del blueprint sin ningún push a producción — este es un buen cierre de sesión, momento fuerte para que Mario revise antes de que siga creciendo. El CSP no se pudo probar de punta a punta contra el hosting real (Apache en Hostinger) sin desplegar — es correcto sintácticamente y cubre los recursos externos reales que usa la app hoy, pero conviene confirmarlo la primera vez que se suba. Siguiente: Paso 27 (salud, monitoreo, respaldos) sin bloqueos, o evaluar el push conjunto de todo lo acumulado. |
| 2026-09-09 | Paso 25 (Fase D) | **Altura/peso base/fuma editables (paciente y staff) + export CSV de pacientes para admin.** `User.medicalInfo` gana `heightCm`, `baseWeightKg`, `smoker`. Descubrí que los dos endpoints de edición que ya existían (`PUT /auth/profile` para autoedición, `PUT /users/:id` para que el staff edite a un paciente) ya aceptaban `medicalInfo` completo — no hizo falta tocar el backend para el guardado, solo el modelo. **Punto crítico que tuve que cuidar en el frontend:** como esos endpoints reemplazan `medicalInfo` entero (no hacen merge por campo), un formulario que solo mandara altura/peso/fuma habría borrado las condiciones crónicas, alergias y lesiones ya cargadas de cada paciente — el componente nuevo (`ClinicalBaselineFields.tsx`, compartido entre el perfil del paciente y la ficha que ve el staff) siempre parte de `{...medicalInfo}` y solo pisa los 3 campos nuevos; verifiqué explícitamente contra la base real que editar altura no borra el resto. La edad se calcula desde `dateOfBirth` (no se guarda, como pedía el blueprint). Export: `GET /api/users/export?format=csv` (nuevo, solo admin) — CSV escrito a mano (mismo criterio que `sanitizeMongo`/`escapeHtml` del Paso 06, sin agregar una dependencia por un formato tan simple) con datos base + profesional asignado + plan activo + sesiones usadas/totales, **sin `medicalInfo`** (minimización de datos sensibles). Botón "Exportar CSV" en `PatientList.tsx`, visible solo para admin, descarga el archivo vía Blob. Verificado de punta a punta contra la base real: 403 para un profesional (no admin), CSV con el contenido correcto y confirmé por grep que no aparece ningún dato médico sensible, y en el navegador el flujo completo de editar altura/peso/fuma sin perder el resto de la info médica. 4 pruebas nuevas para el export, 109 pruebas de backend en verde, tsc y build limpios. | Sonnet 5 | (pendiente) | Va junto con los Pasos 15-24. La parte de "citas del mes" que mencionaba el blueprint junto al export de pacientes **no se implementó** — el criterio de aceptación del paso (`Done when`/`Verify`) solo pedía y probaba el export de pacientes; un export de citas por separado queda para cuando Mario lo pida explícitamente, no se inventó una ruta/forma no especificada. Esta sesión ya acumula 9 pasos del blueprint sin ningún push a producción. Siguiente candidato sin bloqueos: Paso 26 (PWA pulida y headers del frontend) — Pasos 21/22 siguen esperando la cuenta de Cloudflare R2. |
| 2026-09-09 | Paso 24 (Fase D) — **cierra el feature de wellness (Pasos 23+24)** | **Vista de tendencias de bienestar para staff/admin.** `GET /api/wellness/trends` (nuevo): resumen semanal por paciente (promedio, cuántos check-ins, el último, alerta si el promedio < 2.5) — el profesional ve solo a sus pacientes asignados (mismo criterio de pertenencia que el resto de la app), el admin los ve todos; los pacientes sin ningún check-in esa semana se omiten en vez de listar filas vacías, y la lista queda ordenada con los promedios más bajos primero. `GET /api/wellness/patient/:patientId` (nuevo, con pertenencia vía `authorizePatientAccess`): historial completo de un paciente. Frontend: card "Bienestar de pacientes" en el dashboard de staff/admin (fila con borde rojo + badge "Requiere atención" cuando corresponde, clic navega a la ficha del paciente) y pestaña nueva "Bienestar" en `PatientDetail.tsx` con gráfico de tendencia (Recharts, ya era una dependencia existente del proyecto — no se agregó nada nuevo) de las 5 métricas + lista del historial con notas. Verificado de punta a punta contra la base real y en el navegador: el endpoint de tendencias respeta el alcance por profesional (confirmé el query exacto), marca correctamente la alerta, y el flujo completo dashboard → clic en fila → ficha del paciente → pestaña Bienestar → gráfico con datos reales funciona. 3 pruebas nuevas (13 en el archivo de wellness en total), 105 pruebas de backend en verde, tsc y `npm run build` limpios. | Sonnet 5 | (pendiente) | **Con esto, el feature de wellness (Pasos 23+24) queda completo de punta a punta** — modelo, check-in del paciente, alerta automática, y las dos vistas de tendencia para staff/admin. Va junto con los Pasos 15-23. **Siguen bloqueados los Pasos 21/22** (archivos médicos, necesitan que Mario cree la cuenta de Cloudflare R2). Esta sesión ya acumula 8 pasos del blueprint sin ningún push a producción — es un buen momento para que Mario revise y decida qué desplegar antes de seguir. Siguiente candidato sin bloqueos: Paso 25 (historial clínico completo + export CSV) o Paso 26 (PWA pulida). |
| 2026-09-09 | Paso 23 (Fase D) | **Check-in diario de bienestar — el paciente responde 5 sliders (sueño, energía, tranquilidad, cuerpo/dolor, ánimo) desde el dashboard, con alerta automática al profesional si el promedio del día es bajo.** Se saltaron los Pasos 21/22 (archivos médicos en R2): necesitan que Mario cree la cuenta de Cloudflare primero, es un bloqueo real de infraestructura, no de código — se sigue avanzando con lo que no depende de eso. `models/WellnessCheckin.js`: `patient`, `date` ("YYYY-MM-DD" en hora de Santiago), `sleep`/`energy`/`stress`/`soreness`/`mood` (1-5) + `notes`, índice único `{patient, date}` — un check-in por día garantizado por la base de datos, no por el frontend. Las 5 métricas se guardan en la misma dirección (1=mal, 5=bien): la UI pregunta "tranquilidad"/"cuerpo" en sentido inverso ("qué tan tranquilo", "cuánto dolor NO tienes") en vez de "estrés"/"dolor" directo, para que el promedio simple sirva de alerta sin invertir nada al calcularlo. `POST /api/wellness` (paciente, con plan activo — mismo gate que mediciones/ejercicios) y `GET /api/wellness/me` (el check-in de hoy, o null). Si el promedio < 2.5 → `notify(profesional, 'wellness_alert')` (Paso 19). **Se agregó `todayInSantiago()`** a `utils/timezone.js`: el índice único necesita la fecha de HOY en Chile calculada de forma robusta — `nowInSantiago()` (Paso 17) da un Date correcto para comparaciones de hora, pero convertirlo a "YYYY-MM-DD" con `.toISOString()` puede correrse un día si el proceso corre en una zona horaria local distinta a UTC (este mismo entorno de desarrollo corre en `America/Santiago`, lo confirmé mientras depuraba un problema no relacionado en el Paso 17) — `todayInSantiago()` formatea directo con `Intl` sin pasar por ese rodeo. Frontend: card "¿Cómo te sientes hoy?" (`WellnessCheckinCard.tsx`), destacada arriba de todo en el dashboard del paciente — formulario si no ha respondido hoy, resumen de solo lectura si ya respondió, mensaje de renovación si el plan venció. **Bug de contraste real encontrado y corregido en el camino:** los números de los sliders usaban `text-secondary`, que en el tema de este proyecto es un tono casi blanco pensado para fondos, no para texto (`hsl(194 45% 92%)`) — quedaban prácticamente ilegibles; cambiado a `text-teal-600`. Verificado de punta a punta contra la base real y en el navegador (móvil): el formulario se ve y funciona bien, el promedio bajo (probé con los 5 sliders en 1) generó la alerta correcta al profesional, el segundo check-in del mismo día respondió 409, y un paciente sin plan activo recibió 403 con el mensaje de renovación. 7 pruebas nuevas, 102 pruebas de backend en verde, tsc limpio. | Sonnet 5 | (pendiente) | Va junto con los Pasos 15-20. **Pasos 21/22 siguen bloqueados** — cuando Mario cree la cuenta de Cloudflare R2 se puede retomar ahí. Siguiente candidato sin bloqueos: Paso 24 (dashboards de tendencias de wellness para staff/admin) o Paso 25 (historial clínico + export CSV). Sesión ejecutada de forma autónoma, ya son 7 pasos del blueprint en esta sesión sin ningún push a producción — en algún momento conviene que Mario revise y decida qué desplegar antes de seguir acumulando. |
| 2026-09-09 | Paso 20 (Fase D) — **cierra el sistema de notificaciones (Pasos 19+20)** | **Campanita de notificaciones en el header, visible para las 3 vistas (paciente, profesional, admin) porque comparten el mismo `AppLayout`.** `NotificationBell.tsx`: badge rojo con no-leídas (oculto en 0), panel desplegable con título/cuerpo/tiempo relativo (`date-fns` + locale `es`) y punto de color en las no leídas, botón "Marcar todas leídas" que actualiza el badge al instante sin recargar, clic en una notificación la marca como leída y navega a su `link` si tiene uno. Polling cada 60s + refetch al cambiar de ruta (`useLocation`). Mobile-first: el panel usa `calc(100vw-2rem)` en celular y un ancho fijo en escritorio. `src/services/notificationService.ts` + tipo `Notification`/`NotificationType` nuevos en `types/index.ts`, consumiendo los endpoints del Paso 19 tal cual. Verificado en el navegador real, en móvil y escritorio, con notificaciones reales creadas directamente en la base: badge con el conteo correcto, panel con las 3 notificaciones en el orden correcto (más reciente primero), "marcar todas leídas" deja el badge en 0 y lo confirmé también contra el backend (no es solo optimismo del estado local), clic en una notificación navega a su link Y la marca leída (confirmado en el backend), estado vacío "No tienes notificaciones" cuando corresponde. `npm run build` y tsc en verde, sin tocar el backend del Paso 19. | Sonnet 5 | (pendiente) | **El sistema de notificaciones (Pasos 19+20) queda completo y funcional**, pero como todo lo de esta sesión, sin subir a producción. Va junto con los Pasos 15-19. Sesión ejecutada de forma autónoma — este es un buen punto para que Mario revise antes de seguir sumando pasos sin desplegar nada. Siguiente: Paso 21 (archivos médicos en R2) requiere que Mario cree la cuenta de Cloudflare primero — es un paso que necesita su intervención antes de poder avanzar en código, así que conviene pausar aquí y esperar su turno. |
| 2026-09-09 | Paso 19 (Fase D) | **Sistema de notificaciones — modelo, servicio y los 6 eventos cableados (backend, sin campanita todavía — eso es el Paso 20).** `models/Notification.js` (`user`, `type` enum de 6 valores, `title`, `body`, `link`, `read`, `readAt`, índice `{user,read,createdAt}`) + `services/notificationService.js` con `notify(userId, type, {title,body,link})`: crea el registro in-app siempre, y espeja por email (Resend, `sendNotificationEmail` nuevo en `emailService.js`) solo para `plan_expiring`/`plan_expired`/`wellness_alert`/`evolution_updated` — `appointment_booked`/`appointment_cancelled` quedan fuera a propósito porque `appointmentController.js` ya manda su propio email específico en el mismo momento (duplicaría el correo, no la notificación in-app). Endpoints nuevos `GET /api/notifications`, `GET /unread-count`, `PUT /:id/read`, `PUT /read-all` — el usuario siempre forzado a `req.user._id` en la query, nunca en el body (nadie puede marcar como leída una notificación ajena adivinando el id). **Eventos cableados:** paciente registra/edita medición o ejercicio → notifica a `assignedProfessionalId` (verifiqué antes de tocar `evaController.js` que EVA/dolor es admin/profesional-only, sin ruta de escritura para el paciente — no tenía sentido cablearlo ahí, a pesar de que el blueprint lo mencionaba); cita agendada/cancelada → notifica al profesional (mismo criterio que el email existente, sin importar quién la creó); `scripts/expirePlans.js` reescrito para notificar `plan_expiring` (paciente + todos los admins activos, una vez por plan, 5 días antes) y `plan_expired` (ídem, al momento de vencer) — se agregaron `expiringNotifiedAt`/`expiredNotifiedAt` a `ClientPlan` para que el cron no repita el aviso si corre varias veces mientras el plan sigue en esa ventana (verificado corriendo el script dos veces seguidas: la segunda vez no crea nada nuevo). 7 pruebas nuevas (`backend/tests/notifications.test.js`) + verificado de punta a punta contra la base real: los 4 eventos disparan la notificación correcta, un usuario no puede marcar como leída la notificación de otro (404), y el cron de vencimiento es idempotente. 95 pruebas de backend en verde. | Sonnet 5 | (pendiente) | **Sigue sin existir la campanita** (badge + panel desplegable en el header) — eso es el Paso 20, backend listo para que la construya sin tocar nada más aquí. **Va junto con los Pasos 15-18 al hacer push** — nada de esta sesión está en producción. Sesión ejecutada de forma autónoma. Siguiente: Paso 20 (campanita, frontend) para que este trabajo sea visible para alguien, o evaluar el push conjunto de los Pasos 15-19. |
| 2026-09-09 | Paso 18 (Fase C) | **Fin de los precios falsos en la app del paciente, y número de WhatsApp centralizado en toda la app.** Eliminado `src/pages/patient/Subscription.tsx` (`/app/planes`) — mostraba precios y un botón "Contratar Plan" que solo abría WhatsApp con un mensaje armado, sin ninguna suscripción real; además usaba los tipos de plan legacy (`entrenamiento-2x`/`3x`) que ya no existen desde el Paso 16. Ruta y entrada del sidebar retiradas — `/app/planes` ahora cae en el 404 de la SPA. El número de WhatsApp (`56956286651`), antes duplicado en 12 archivos en 3 formatos distintos (link `wa.me`, texto legible `+56 9 5628 6651`, `tel:`), pasa a tener una sola fuente: `src/config/contact.ts` (ya existía con un comentario documentando la deuda pendiente — ahora sí se completó). **Decisión tomada sin preguntar, documentada aquí:** el precio `$89.990` que pedía eliminar el comando de verificación del blueprint sigue en `src/components/Pricing.tsx` — esa es la landing pública de marketing (`primefh.cl`, antes de iniciar sesión), no la app autenticada; la regla de negocio ("los precios NO se muestran en la app") es sobre el portal del paciente, no sobre el sitio de ventas. Quitar el precio ahí sería contraproducente (es la página que existe para vender). Si Mario quiere que también se quite de ahí, se puede hacer aparte. **La alerta de vencimiento (fila amarilla ≤5 días, roja vencida) ya existía y funciona** en `Plans.tsx` y `PatientList.tsx` — no hizo falta tocar nada. La campanita de notificaciones completa (in-app + email para "plan por vencer"/"plan vencido") está totalmente sin construir (ni frontend ni backend) — pero eso es un paso propio y numerado más adelante en el blueprint (Pasos 19/20), no se tocó aquí para no adelantarse fuera de orden. Verificado: `npm run build` sin errores, sin precios ni número de WhatsApp hardcodeado fuera de `Pricing.tsx` y `config/contact.ts`, navegador real confirma `/app/planes` → 404 y los links de WhatsApp/teléfono renderizan con el formato correcto. | Sonnet 5 | (pendiente) | **Va junto con los Pasos 15-17 al hacer push.** Sesión ejecutada de forma autónoma. Siguiente: Fase D (features faltantes) — o evaluar el push conjunto de los Pasos 15-18, que es donde conviene pausar y que Mario revise antes de seguir sumando cambios sin desplegar. |
| 2026-09-09 | Paso 17 (Fase C) | **Ventana de agendamiento a 4h con hora real de Chile, reserva masiva con todas las reglas, y un bug grave de capacidad corregido.** (a) `PATIENT_BOOK_AHEAD_HOURS` 24→4, pero el cambio real fue más profundo: el servidor compara `now` contra la hora de la cita sin conversión de zona horaria — en producción (Railway, UTC) esto desfasaba las reglas de agendamiento/cancelación varias horas respecto a la hora real de Puerto Montt. Se creó `nowInSantiago()` (`backend/utils/timezone.js`, vía `Intl.DateTimeFormat` con `timeZone:'America/Santiago'`, sin dependencia nueva) y se aplicó en las 3 comparaciones de hora del flujo de citas (crear, reserva masiva, cancelar) y en el hook `pre('save')` del modelo `Appointment`. **(b) Bug grave encontrado y corregido — la reserva masiva ('Horario predeterminado') nunca aplicaba el motor de descuento de sesiones**: el chequeo de saldo se llamaba sin el parámetro `type` (por lo que nunca bloqueaba nada) y ninguna sesión se descontaba nunca al crear las citas — un paciente podía reservar sesiones recurrentes ilimitadas gratis. Reescrito `bulkCreateAppointments` para validar y descontar cada cita del lote individualmente (saldo por tipo, 4h, cupo, fecha no pasada), con tope de 20 citas por lote y respuesta `{created, skipped: [{fecha, motivo}]}` (antes `{appointments, errors}` — frontend actualizado a la par). **(c) Bug grave encontrado y corregido — el endpoint de slots que usa la pantalla de reserva marcaba un horario como "lleno" con solo 1 cita**, ignorando la regla de máximo 4 pacientes simultáneos (la lógica correcta de solapamiento ya existía, pero en un endpoint distinto y sin usar — muerto, confirmado por grep, ahora eliminado). Se extrajo `countOverlappingAppointments`/`MAX_PATIENTS_PER_SLOT` a `backend/services/bookingRulesService.js`, única fuente de verdad usada tanto al validar una reserva como al listar los slots disponibles (`availabilityController.getAvailableSlots`). También se agregó el código `BOOKING_WINDOW` al 400 de ventana insuficiente (pedido explícito del blueprint), se corrigieron dos textos de cara al paciente que decían "24 horas" para la regla de **cancelación** (que siempre fue 4h, ya estaban mal antes de esta sesión: `Terms.tsx` y el diálogo de cancelar en `MyAppointments.tsx`), y se limpiaron imports/funciones muertas encontradas en el camino (`getAvailability` duplicado en `appointmentController.js`, `canBeCancelled` del modelo sin ningún consumidor). El script `expirePlans.js` (Paso 17-b) ya existía y no necesitó cambios — sigue pendiente de que Mario configure un Cron Job en Railway. 8 pruebas nuevas (`backend/tests/bookingRules.test.js`) + verificado de punta a punta contra la base real: ventana de 4h con datos reales, slot que se llena recién al 4to paciente, reserva masiva descontando sesión por sesión (2→5 con 3 citas), tope de 20. 88 pruebas de backend y tsc en verde. | Sonnet 5 | (pendiente) | **Va junto con los Pasos 15+16 al hacer push** — ninguno está en producción. **Pendiente de Mario:** crear el Cron Job en Railway (Settings → Cron) para `npm run expire-plans` a las 03:00 America/Santiago. **Hallazgo no resuelto, fuera de alcance de este paso:** el calendario de reserva del paciente (`CalendarView`) deshabilita el día completo de "hoy" sin importar la hora (compara contra medianoche, no contra la hora real) — con la ventana ahora en 4h esto significa que técnicamente se podría reservar el mismo día más tarde, pero la UI no lo permite todavía; requeriría filtrar por horario dentro del día, no por día completo — no se tocó para no ampliar el alcance de este paso. Sesión ejecutada de forma autónoma (Mario pidió seguir avanzando sin necesitar su input). Siguiente: Paso 18 (fin de los precios en la app y consolidación de UI de planes), o evaluar el push conjunto de los Pasos 15-17. |
| 2026-09-09 | Paso 16 (Fase C) | **Retirado por completo el modelo `Plan` legacy — `ClientPlan` queda como única fuente de verdad.** Confirmado antes de empezar: 0 documentos `Plan` en producción (nunca hubo nada que migrar), así que se saltó el script de migración de datos que pedía el blueprint — no tenía sentido escribir un script para migrar cero filas. Backend: eliminados `models/Plan.js`, `controllers/planController.js`, `routes/planRoutes.js` y el mount `/api/plans` (ahora 404); `getPlanInfo` (y sus helpers `MONTHLY_LIMITS`/`countMonthlyAppointments`/`countKineSessions`, confirmados sin otro uso) eliminados de `appointmentController.js` — el frontend pasa a usar `GET /client-plans/balance/:patientId`, que ya existía y ya cubre exactamente lo mismo sobre `ClientPlan`. Frontend: `BookAppointment.tsx` (la pantalla de reserva del paciente), `MyProfile.tsx`, `PatientDetail.tsx` y `BulkBookingDialog.tsx` migrados de `planService`/`Plan` a `clientPlanService`/`ClientPlan`; eliminados `services/planService.ts`, `components/forms/PlanForm.tsx` y los tipos `Plan`/`CreatePlanData`/`PlanRestrictions`. **Tres bugs reales encontrados y corregidos en el camino** (no eran parte del plan original, aparecieron al leer el código antes de tocarlo): (1) `PatientDetail.tsx` y `MyProfile.tsx` leían `activePlan.type`, un campo que nunca existió (el campo real era `planType`/ahora `serviceType`) — el "Tipo" de plan salía en blanco siempre; (2) mismo bug en `BulkBookingDialog.tsx`, en el texto de rango de fechas; (3) **más grave** — `BulkBookingDialog.tsx` mandaba `type: 'entrenamiento'` fijo para TODAS las reservas masivas sin importar el plan real del paciente, lo que significaba que un paciente de kinesiología usando "Horario predeterminado" habría recibido 403 en cada cita (el motor de descuento del Paso 15 es consciente del tipo) — corregido a `type: plan.serviceType`. También se sacó el atajo que dejaba agendar a cualquier usuario activo sin ningún plan (contradecía la regla de negocio "sin plan activo no se agenda"; el backend ya lo bloqueaba, pero la pantalla insinuaba que sí se podía). El calendario de reserva pasó a mostrar siempre 2 meses (antes era 1 o 2 según el tipo de plan, una distinción que ya no aplica: ambos tipos comparten el mismo ciclo de 30 días desde el Paso 15). Verificado de punta a punta contra la base real con datos de prueba (creados y eliminados en la misma sesión): banner de saldo correcto para kinesiología y entrenamiento en las 3 pantallas (reservar, mi perfil, ficha admin), botón "Asignar plan" cuando no hay plan activo, `GET/POST /api/plans` → 404, `grep models/Plan` → 0 coincidencias. 80 pruebas de backend y compilación de TypeScript en verde. | Sonnet 5 | (pendiente) | **Va junto con el Paso 15 al hacer push** — ninguno de los dos está en producción todavía. Sesión ejecutada de forma autónoma (Mario pidió seguir avanzando sin necesitar su input); un intento de logueo con la cuenta real de Mario para probarlo visualmente falló por contraseña (pendiente confirmar con él antes de tocarla). Siguiente: cuando Mario tenga tiempo, decidir el push conjunto de Pasos 15+16 a producción, y avanzar con el Paso 17 (ventana de 4h, cron de expiración, recurrente completa). |
| 2026-09-08 | Operativo (fuera de numeración) — destraba el Paso 15 | **Clasificación de planes para los 40 pacientes reales.** El Paso 15 (motor de descuento, `cb4fa7a`) quedó sin subir a producción porque hoy hay 0 `ClientPlan` reales — nunca se llevó ese dato en ningún sistema. Mario decidió no migrar datos que nunca existieron, sino automatizar la asignación hacia adelante: kinesiología siempre 10 sesiones fijas, entrenamiento a elección del catálogo (4/8/12/16). Se construyó: (1) **permiso ampliado** — `POST /client-plans` ahora acepta también a `professional`, restringido a sus propios pacientes asignados vía el middleware `authorizePatientAccess` ya existente (`backend/routes/clientPlanRoutes.js`) — no hizo falta tocar el controller, el middleware ya sabía leer `patientId` del body; (2) pantalla nueva `ClassifyPlans.tsx` (`/app/admin/planes/clasificar`) — un clic por paciente sin plan activo; (3) card "Plan inicial" en `RegisterPatient.tsx` para pacientes nuevos (Kinesiología / Entrenamiento / Asignar después), con manejo de error no bloqueante si el plan falla tras crear el paciente. **Verificado de punta a punta contra la base real** con usuarios de prueba (creados y eliminados en la misma sesión): un profesional puede asignar plan a su propio paciente (201), no puede a uno ajeno (404, no 403 — no confirma que existe), el dueño real sí puede (201), y el admin no tiene restricción. Probado también en el navegador en viewport móvil (375×812): las 3 pantallas renderizan y el flujo completo (crear paciente → asignar plan → aparece en Planes) funciona. **Bug de layout encontrado y corregido en el camino:** los 3 botones de "Plan inicial" se superponían en móvil con `grid-cols-3` fijo — se cambió a `grid-cols-1 sm:grid-cols-3`. | Sonnet 5 | (pendiente) | **`cancelClientPlan` sigue admin-only**, sin cambios — Mario solo pidió esto para registrar/asignar, no para cancelar. El catálogo general (`PLAN_CATALOG`) no cambió: la fijación a 10 es solo para estas dos pantallas de asignación inicial/masiva. Con esto disponible, Mario puede clasificar a los 40 pacientes reales cuando quiera — después de eso, retomar la conversación sobre subir el Paso 15 a producción. |
| 2026-07-17 | Infraestructura/Deploy | Diagnóstico y arreglo completo del sitio caído: (1) Railway — el trial gratuito había expirado, se pagó el plan y redesplegó solo; (2) `app.primefh.cl` nunca se había publicado — se creó el subdominio en Hostinger, se corrigió `.env.production` (faltaba `/api` en `VITE_API_URL`), se compiló el frontend y se subió manualmente vía File Manager; (3) bug de CORS — `FRONTEND_URL` en Railway apuntaba a sí mismo en vez de a `https://app.primefh.cl`, corregido y redesplegado. Se limpiaron archivos basura sueltos en `backend/`. | Sonnet 5 | `744264d` | Login con la cuenta semilla (`mario@primefh.cl`) dio credenciales inválidas — revisar/resetear si se necesita acceso admin. |
| 2026-07-17 | Documentación | Se reescribieron `CLAUDE.md` y `STATUS.md` completos: el `CLAUDE.md` anterior describía un stack (Next.js + Supabase) que nunca se construyó — se reemplazó por el stack real (Vite + Express + MongoDB) y el modelo de negocio objetivo. `STATUS.md` se rehízo con un inventario verificado del código (qué existe, qué existe con reglas distintas, qué falta) y un checklist de Etapas 2 a 7. | Sonnet 5 | (pendiente) | Definir con Mario: ¿se ajustan los planes/ventana de agendamiento a las reglas objetivo, o las reglas objetivo se ajustan a lo ya construido? |
| 2026-09-08 | Paso 11 (Fase B, EN CURSO) | **Nodemailer reemplazado por Resend — código en producción, falta la cuenta de Resend.** El servicio SMTP viejo nunca terminó de configurarse bien de todos modos (las variables de `.env.example` ni siquiera coincidían con las que el código real usaba). `services/emailService.js` nuevo: `sendEmail()` vía `fetch` directo a la API de Resend (sin instalar su paquete oficial — mismo criterio que `sanitizeMongo` del Paso 06), fire-and-forget real: si falta `RESEND_API_KEY` o la petición falla, se registra un aviso y el flujo que disparó el correo sigue funcionando igual. **Verificado end-to-end:** creé una cita real contra la base sin la API key configurada — respondió 201 normal, solo dejó el aviso en el log, nada se rompió. Los 3 emails de citas se migraron con la misma firma exacta (appointmentController.js solo cambió el import). Se eliminó `utils/emailService.js` y la dependencia `nodemailer` del todo (confirmado por grep: cero rastros). 12 pruebas nuevas con `fetch` mockeado (falta la key, Resend responde error, falla la red, escape de HTML con `<script>`/`<img onerror>` de prueba) — 51 pruebas en verde en total. Se corrigió de paso `FRONTEND_URL` en `.env.example` (decía `localhost:5173`, el puerto real es `8080` — ya se había corregido en `config/allowedOrigins.js` del Paso 09, faltaba aquí). Desplegado a producción y verificado sano (backend responde 200, CORS intacto). | Opus 5 | `80671e4` | **Pendiente de Mario, paneles externos:** crear cuenta en Resend → agregar dominio `primefh.cl` → copiar registros SPF/DKIM a Hostinger → verificar → crear API key → ponerla en Railway como `RESEND_API_KEY`. Sin esa variable, el sistema sigue funcionando normal, solo no se envían los correos de notificación de citas (con aviso en el log, no en silencio). Cuando esté lista la key: `node backend/scripts/testEmail.js <tu email>` confirma que llega a inbox. **También sigue pendiente el Paso 09** (Railway aún no emite el certificado de `api.primefh.cl` — más de 12 horas desde que se configuró el DNS, ya fuera del tiempo normal de espera; conviene que Mario revise si hay algún error visible en el panel de Railway, o intente quitar y volver a agregar el dominio). Siguiente: Paso 12 (onboarding por invitación). |
| 2026-09-07 | Paso 10 (Fase B) | **Frontend conectado a la sesión nueva: access token en memoria, cero PHI en localStorage.** Se implementó en **Plan Mode** por ser un cambio de arquitectura que toca 6 archivos y el flujo de sesión de todos los usuarios. `api.ts`: `withCredentials:true` (faltaba — sin esto el navegador nunca manda la cookie), token en variable de módulo, `refreshAccessToken()` con promesa compartida (varias peticiones que fallan con 401 a la vez disparan un solo refresh, no uno cada una). `AuthContext.tsx`: "silent login" al montar (refresh con la cookie → `/auth/me`), `login()` ahora retorna el `User` para decidir el redirect por rol sin esperar el `setState`, `logout()` avisa al backend de verdad. **Bug del propio blueprint corregido:** `LoginDual.tsx` decidía el redirect post-login por la *pestaña* usada (Paciente/Personal), no por el rol real — verificado con un profesional entrando por la pestaña "Paciente": ahora termina en su panel correcto. Se eliminó `Login.tsx` (código muerto, no montado en ninguna ruta) y `loginWithToken`/`isAuthenticated()`/`getToken()`/`getStoredUser()` (sin consumidores, confirmado por grep exhaustivo de todo `src/`). Verificado en el navegador real (Chrome vía Claude Browser): login → `localStorage` vacío, recarga completa → sigue logueado, logout → recarga → pide login de nuevo (la cookie quedó revocada de verdad en el backend, no solo limpiada en el cliente). **Decisión de Mario:** sin coordinación entre pestañas del navegador para el refresh (riesgo aceptado: caso raro, bajo impacto). `/staff-dashboard` (312 líneas, sin ningún guard) se dejó fuera de alcance — hallazgo aparte, no se tocó. | Opus 5 | `53d3340` (tag `step-10-frontend-auth`) | **NO desplegar este build a Hostinger todavía.** En producción, `app.primefh.cl` y el backend de Railway siguen siendo dominios distintos hasta que el Paso 09 cierre (dominio `api.primefh.cl`, aún esperando que Railway emita el certificado TLS) — subir este frontend antes rompería la sesión en producción (la cookie de refresh no viaja cross-site, los usuarios quedarían deslogueados cada hora). Funciona sin problema en local porque frontend y backend comparten origen de desarrollo. Siguiente: cerrar el Paso 09, LUEGO desplegar el Paso 10 a Hostinger, y seguir con el Paso 11 (Resend). |
| 2026-09-08 | Paso 12 (Fase B) — código listo, push pendiente | **Invitación por email para dar de alta pacientes.** Ya no hay contraseña puesta por el profesional/admin: la cuenta nace con una aleatoria que nadie conoce, y el paciente recibe un link de un solo uso (7 días) para elegir la suya. `POST /auth/accept-invite/:token` colapsa los 3 casos inválidos (nunca existió, ya usado, vencido) en la misma respuesta genérica `INVITE_EXPIRED` — mismo principio de no dar pistas que ya usa `forgotPassword`. `POST /users/:id/resend-invite` (admin o profesional dueño) también sirve, a propósito, como reset de contraseña asistido sobre una cuenta ya activa. **Se generaliza a cualquier rol, no solo pacientes** — un admin creando a otro profesional también pasa por invitación, porque limitarlo a pacientes habría dejado sin forma de entrar a una cuenta de staff creada sin contraseña conocida (bug real que detecté y corregí durante la implementación). Verificado con 15 pruebas nuevas (66 en total) y de punta a punta contra la base real: crear paciente → no puede loguearse con nada → acepta la invitación real → quedó con sesión iniciada → reusar el mismo link falla → ahora sí puede loguearse con su contraseña. | Opus 5 | `4912509` (tag `step-12-invite`, **local, sin `git push` todavía**) | **Por qué no subí esto a producción:** este cambio, tal como está, deja el alta de pacientes nuevos inutilizable en producción hasta que se complete DOS cosas pendientes — (1) `RESEND_API_KEY` en Railway (Paso 11, Mario debe configurar Resend) para que el correo de invitación realmente llegue, y (2) la pantalla `/invitacion/:token` en el frontend (Paso 13, todavía no implementado) para que el paciente pueda abrir el link y elegir su contraseña. Sin ambas cosas, cualquier paciente que Mario o un profesional den de alta en producción quedaría sin ninguna forma de entrar. Subir el código ahora no rompe lo que ya funciona (nadie usa esta ruta todavía), pero **antes de hacer push hay que decidir con Mario si conviene esperar a tener el Paso 13 listo también**, para no dejar una ventana donde crear un paciente en producción no sirva de nada. Siguiente: Paso 13 (frontend de este mismo flujo), y luego evaluar el push conjunto. |
| 2026-09-08 | Paso 13 (Fase B) | **Pantallas de invitación y reset por email, de punta a punta.** `AcceptInvite.tsx` (`/invitacion/:token`) y `ResetPassword.tsx` (`/restablecer/:token`): formulario optimista (no existe endpoint para verificar un token sin gastarlo), y solo caen a la pantalla de error con WhatsApp si el backend responde el código específico de "vencido/usado" — cualquier otro error se queda en el mismo formulario. `RecoverPassword.tsx` reescrito: de "solo WhatsApp" a pedir el email real, con WhatsApp como red de seguridad secundaria. Se conectó la pieza de backend que quedaba con un TODO explícito desde el Paso 01: `forgotPassword` ahora sí manda el correo de reset (Resend), y `resetPassword` incluye `user` en su respuesta (mismo shape que `login`/`acceptInvite`) para poder redirigir por rol real. Botón "Reenviar invitación" en la ficha del paciente, con confirmación. **Hallazgo corregido de paso:** `RegisterPatient.tsx` todavía pedía una contraseña obligatoria que el backend ignora desde el Paso 12 — confundía al profesional con un campo que no hacía nada; se eliminó. Verificado en el navegador real, de punta a punta, los 4 flujos completos (invitación, reuso de link, reset, reenvío). Build sin errores, 66 pruebas de backend en verde. | Opus 5 | `b59a3cc` (tag `step-13-invite-ui`) | **Backend ya subido a producción** (el cambio es aditivo y seguro: si falta `RESEND_API_KEY`, sigue respondiendo igual, solo no manda el correo). **El frontend NO se subió a Hostinger** — mismo motivo que los Pasos 10 y 12: depende de que el dominio `api.primefh.cl` esté funcionando (Paso 09, aún pendiente) y de que Resend esté configurado en Railway (Paso 11) para que los correos realmente lleguen. Subir el frontend antes dejaría a un paciente nuevo sin ninguna forma de completar su alta. Siguiente: cerrar el Paso 09 (dominio), configurar Resend, y recién ahí evaluar el despliegue conjunto del frontend acumulado (Pasos 10 + 12 + 13). |
| 2026-09-08 | Paso 14 (Fase B) — **cierra la Fase B** | **Limpieza final: mocks fuera, y un hueco de guards cerrado.** Se eliminó `StaffDashboard.tsx` y `components/staff/*` (mocks con datos ficticios, sin guard de ruta, confirmados sin otro consumidor) — `/staff-dashboard` ahora cae en el 404 de la SPA. Las 7 rutas de paciente quedaron protegidas por `RoleRoute roles={['patient']}`, igual que ya estaban las de admin. **Problema real encontrado y corregido en el camino:** `RoleRoute` redirigía siempre a un `/app/dashboard` fijo — con las rutas de paciente ahora también protegidas por el mismo guard, eso hubiera causado un loop infinito de redirects para cualquier staff que cayera ahí. Se cambió el fallback para que dependa del rol real (paciente → dashboard, staff → admin) — verificado en el navegador real en ambos sentidos, sin loops. Con los mocks fuera, se retiraron del `package.json` del frontend `chart.js`/`react-chartjs-2` (sin otro uso) y `googleapis`/`google-auth-library` (error de un commit viejo — nunca pertenecieron al frontend). El bundle de producción bajó ~213kB. | Opus 5 | `2a76456` (tag `step-14-guards`) | **FASE B COMPLETA (Pasos 08-14).** Este paso no toca backend, así que no hay nada nuevo que desplegar en Railway. El frontend acumulado de toda la Fase B (Pasos 10, 12, 13, 14) sigue esperando a subirse a Hostinger junto — depende de que el dominio `api.primefh.cl` (Paso 09) esté funcionando y de que Resend (Paso 11) esté configurado en Railway. Siguiente: Fase C (planes y agenda), empezando por el Paso 15 (motor de descuento de sesiones) — o cerrar antes los pendientes de infraestructura (dominio + Resend) para poder desplegar todo el frontend acumulado. |
| 2026-09-07 | Paso 09 (Fase B, EN CURSO) | **CORS restringido y defensa CSRF — código listo, falta la parte de infraestructura.** `config/allowedOrigins.js` centraliza la allowlist (antes repetida): se retiraron `localhost:5173` (nunca se usó — el proyecto real corre en el puerto 8080, `vite.config.ts`) y `localhost:8081` (sin justificación). CORS es ahora una allowlist exacta; un Origin ajeno nunca recibe el header `Access-Control-Allow-Origin` (verificado en local y en producción tras el despliegue). Nueva defensa: `POST /auth/refresh` exige que el header `Origin` sea el nuestro — es la única ruta pública que actúa solo con la cookie, sin contraseña ni Bearer token (verificado: 403 sin Origin o con uno ajeno, 200 con el Origin real). 44 pruebas en verde (39 previas + 5 nuevas). Confirmado antes de subir: `FRONTEND_URL` ya estaba bien configurado en Railway, así que el despliegue no rompió nada. **Falta la parte de infraestructura, que le corresponde a Mario:** crear el dominio `api.primefh.cl` en Railway (Settings → Domains) y el registro CNAME correspondiente en Hostinger; luego actualizar `VITE_API_URL` en el build de producción del frontend. Sin esos dos pasos, el Paso 09 no se puede dar por cerrado ni tagear `step-09-domain`. | Opus 5 | `a71fec0` | **Bloqueado en la parte de Mario** (paneles de Railway/Hostinger, fuera del alcance de Claude Code). Cuando lo confirme: verificar `https://api.primefh.cl/` con TLS válido, hacer el commit/tag de cierre, y recién ahí seguir con el Paso 10 (frontend: token en memoria). |
| 2026-09-07 | Paso 08 (Fase B) | **Refresh tokens rotativos y revocables.** El JWT de 7 días en localStorage se reemplaza por dos tokens: acceso (1h, en el body, igual que antes) + refresh (opaco, 30 días, en cookie `HttpOnly` que JavaScript no puede leer). Cada uso del refresh lo rota (destruye el viejo, emite uno nuevo); si alguien reusa uno ya rotado, se interpreta como robo y se revoca **toda esa cadena de sesión** (confirmado con la base real: las demás sesiones del mismo usuario en otros dispositivos no se ven afectadas — la revocación es por sesión, no por usuario). **Decisiones de Mario:** cada sesión vence a los 30 días fijos desde el login (no una ventana que se renueva sola, para limitar el daño de una sesión robada que nadie nota); al cambiar la contraseña, el dispositivo donde se cambia se queda conectado y todos los demás se desconectan (verificado). Se implementó en **Plan Mode** por ser un cambio de arquitectura de sesión. Este paso es 100% backend y aditivo — el frontend actual (localStorage) sigue funcionando exactamente igual; la migración del frontend es un paso posterior. Se escribió a mano la lectura de la cookie (`backend/utils/cookies.js`) en vez de instalar `cookie-parser`, mismo criterio que `sanitizeMongo` del Paso 06. 39 pruebas en verde (20 previas + 19 nuevas). | Opus 5 | `49d8d46` (tag `step-08-refresh`) | **Pendiente de Mario:** cambiar `JWT_EXPIRE` a `1h` en las variables de entorno de Railway (Claude Code no toca secretos de producción). **Limitación conocida, no resuelta aquí:** la cookie no viajará de forma confiable en producción hasta el Paso 09 (`app.primefh.cl` y `prime-saas-production.up.railway.app` son dominios distintos hoy). Siguiente: Paso 09 (dominio `api.primefh.cl` — requiere que Mario cree un registro CNAME en Hostinger). |
| 2026-09-06 | Paso 07 (Fase A) — **cierra la Fase A** | **Google Calendar en cuarentena + red de pruebas automáticas.** La integración con Google respondía a todo con 403 `FEATURE_DISABLED`: nunca funcionó (guardaba los tokens en un campo inexistente en el modelo, descartados en silencio) y era insegura — un único cliente OAuth compartido entre todas las peticiones (riesgo de aplicar las credenciales de una persona a la petición de otra), sin protección CSRF, pidiendo permisos totales sobre el calendario y sin verificar pertenencia al sincronizar. El frontend nunca la llamaba, así que desactivarla no quita nada. **Suite de 20 pruebas con vitest** (`npm test` en /backend, medio segundo, sin base de datos ni servidor): cubre pertenencia, serialización sin secretos, política de contraseñas, bloqueo de comandos de BD y escape de texto. Se instaló solo `vitest`, no las 3 dependencias previstas. También se versionó `backend/package-lock.json`, que estaba excluido del repositorio. **Requisito nuevo de Mario:** el calendario debe servir para él, los kinesiólogos y los pacientes → el Paso 28 se reescribió en dos partes (OAuth para staff, archivos `.ics` para pacientes). | Opus 5 | `583e5c8` (tag `step-07-quarantine`) | **FASE A COMPLETA: los 7 hotfixes de seguridad cerrados y verificados.** Antes de seguir a la Fase B conviene el smoke test de producción (BLUEPRINT §20.1). Siguiente: Paso 08 (refresh tokens, Plan Mode). |
| 2026-09-06 | Paso 06 (Fase A) | **Endurecimiento del servidor.** Límite de intentos con `express-rate-limit`: login 5 cada 15 min por IP+email (un login correcto no gasta intentos), recuperación 3/hora, y un techo general de 300/5min para toda la API — antes se podían probar contraseñas sin parar. Cabeceras de seguridad con `helmet` + `trust proxy` (sin esto el límite sería inservible tras el proxy de Railway). Tope de 1 MB por petición. **Decisión de Mario:** la sanitización NoSQL se escribió a mano (`middleware/sanitize.js`) en vez de usar `express-mongo-sanitize`, que lleva años sin mantenimiento y se rompe con Express 5 — son ~20 líneas legibles y una dependencia menos. También `escapeRegex` en todas las búsquedas (antes buscar `(` rompía la consulta y `(a+)+$` podía colgar el servidor) y `escapeHtml` en los correos (el motivo de una cancelación se incrustaba sin escapar en el correo del profesional). El `errorHandler` ahora respeta el código propio del error (413/400 en vez de 500). Verificado: el 6.º intento de login da 429, las cabeceras están presentes, y ni un profesional ni un paciente logran escaparse de su alcance inyectando operadores en la URL. | Opus 5 | `8b15163` (tag `step-06-hardening`) | **Pendiente de infraestructura:** `backend/package-lock.json` está en `.gitignore`, así que Railway instala las versiones que le toquen en cada despliegue en vez de las probadas aquí. Conviene versionarlo. Siguiente: Paso 07, último de la Fase A. |
| 2026-09-06 | Paso 05 (Fase A) | **Cerrada la escalada de privilegios.** El rol lo decide ahora el servidor, no el formulario: antes `createUser` y `updateUser` leían `role` e `isActive` directo del body, así que un profesional podía crearse una cuenta de administrador, ascender a cualquiera o desactivar al admin. Se pasó a lista blanca de campos en ambos: un profesional solo crea pacientes (asignados a él) y solo edita los suyos (403 ante cuentas del equipo, 404 ante pacientes ajenos); rol, estado y asignación quedan reservados al admin. `DELETE /users/:id` pasó a solo-admin y ahora **desactiva en vez de borrar** — un borrado real destruiría la ficha clínica y el historial de citas, que deben conservarse por normativa. Contraseña mínima de 6 → 8 caracteres (modelo, backend con 400 claro, y formularios). Verificado con 12 pruebas reales contra `prime_fh`: la escalada falla por todos los caminos, el admin conserva sus capacidades y los profesionales siguen trabajando normal. Sin datos de prueba residuales. | Opus 5 | `0027c46` (tag `step-05-roles`) | Bug propio detectado y corregido en el camino: usé `canAccessPatient` en `userController` sin importarlo — falló hacia el lado seguro (bloqueaba), pero devolvía 500 en vez de 404. Siguiente: Paso 06 (rate limiting y hardening). |
| 2026-09-06 | Paso 04 — verificación + hallazgo de infraestructura | **Verificación completa del Paso 04: 24 pruebas reales contra `prime_fh`, todas pasaron.** Un paciente no puede leer ficha, perfil clínico, plan, mediciones, ejercicios, dolor, saldo ni sesiones extra de otro (404 en los 10 casos); sí ve todo lo suyo; un profesional solo accede a sus asignados; nadie puede tocar la agenda de otro profesional (403, y se comprobó que la de Tomás quedó intacta); ningún token sensible sale en las respuestas. **HALLAZGO GRAVE DE INFRAESTRUCTURA:** la app en producción estaba conectada a la base `test` (vacía, 1 usuario) en vez de `prime_fh` (43 usuarios, 39 pacientes) — la `MONGODB_URI` de Railway no incluía el nombre de la base. Los 39 pacientes importados de Airtable nunca habían sido visibles en la app. Corregido en Railway y en el `.env` local. En el proceso Mario pegó la contraseña de MongoDB en el chat y fue **rotada** (esta vez sí correspondía). Se añadió `.env.backup` al `.gitignore`. **Bugs corregidos de paso:** los 2 endpoints que siempre respondían 500 (Paso 07c, adelantado) — impedían que un paciente viera sus propias mediciones y zonas de dolor. | Opus 5 | `0c7ea55` | De los 39 pacientes, 38 tienen profesional asignado y 1 queda en el pool. Reparto: 35 a Mario, 2 a Felipe, 1 a Tomás, 0 a Rafael. **Pendiente:** Mario debe corregir la `MONGODB_URI` de Railway (producción caída con 502 hasta entonces). Siguiente: Paso 05. | **Cerrados los IDOR: ya no se puede leer la ficha de otro paciente cambiando el id en la URL.** Se creó una regla única de pertenencia (`canAccessPatient` / `authorizePatientAccess` en `middleware/auth.js`) y se montó en las **16 rutas** con `:patientId` — incluida `extra-sessions/patient/:patientId`, que el blueprint no había listado. Responde 404 (no 403) para no confirmar que el paciente existe. Además: `GET /api/users` ahora decide el alcance en el servidor (un profesional sin filtro recibía la base COMPLETA de usuarios; el paciente solo puede listar staff con campos mínimos), los GET de registro único de measurements/exercises/eva ahora también validan al profesional, cada profesional solo puede editar su propia disponibilidad, `DELETE /appointments/:id` queda solo-admin, y `User.toJSON` nunca serializa `password`/`resetPasswordToken`/`invite`. Verificado: cobertura de las 16 rutas, 11 casos de la regla con doble de prueba, serialización sin fugas. | Opus 5 | `2184587` (tag `step-04-idor`) | **La prueba HTTP de extremo a extremo NO se corrió** — la BD local rechaza la conexión (contraseña de MongoDB desactualizada en `backend/.env`; producción está bien). Mario optó por verificar manualmente en la app tras desplegar. **Riesgo a vigilar:** los pacientes sin `assignedProfessionalId` quedan invisibles para los profesionales — si pasa, correr `npm run migrate-assigned-professional` (idempotente). **Cambio de comportamiento:** el selector de profesionales del paciente ahora sí muestra a los admin (antes un bug lo impedía). Siguiente: Paso 05. | **`.env.production` sacado del control de versiones — y V7 desmentido.** Antes de pedir la rotación de credenciales, se verificó qué contenía realmente el archivo (extrayendo solo nombres de variable, nunca valores): **una sola variable, `VITE_API_URL`**, que es la URL pública de la API y queda igualmente incrustada en el bundle del frontend. Un barrido del historial completo de git buscando cadenas de conexión de MongoDB, `JWT_SECRET`, `AIRTABLE_API_KEY` y `SMTP_PASS` no encontró ninguna credencial real: todas las coincidencias son ejemplos con usuario/password ficticios en `SETUP.md` y los `.env.example`, y `backend/.env` nunca estuvo trackeado. **Conclusión: nunca hubo filtración → la rotación obligatoria queda cancelada.** El hallazgo V7 del blueprint era un falso positivo (se marcó por el nombre del archivo, sin verificar el contenido); corregido en `BLUEPRINT.md`. | Opus 5 | (ver tag `step-03-secrets`) | **Único pendiente de Mario (no urgente):** verificar en Railway que `JWT_SECRET` sea largo y aleatorio — si fuera una palabra corta o adivinable, alguien podría falsificar sesiones. No es verificable desde el repo. Siguiente: Paso 04 (Opus), el de los IDOR. |
| 2026-08-05 | Paso 02 (Fase A) | **Eliminada la puerta trasera de Airtable.** El login ya no llama a `syncPatientByEmail`: un email desconocido falla con 401 en vez de crear la cuenta con contraseña `123456`. Se eliminó la función completa (su único llamador era el login). **Hallazgo nuevo, fuera del blueprint:** la misma contraseña fija estaba en `scripts/migrate.js` (que Mario ya ejecutó — hay cuentas migradas con esa clave) y en `utils/seed.js`; ambos corregidos para usar contraseñas aleatorias/de entorno. `POST /users/sync-airtable` pasó a solo-admin. Eliminados `scripts/update_passwords.js` (reseteaba TODAS las contraseñas a valores fijos) y `scripts/test_booking_logic.js` (dependía del backdoor). Verificado contra Atlas: 43 usuarios antes y después del intento de login, cuenta no creada, 401. | Opus 5 | `84f6368` (tag `step-02-airtable`) | **Riesgo residual:** pueden existir cuentas reales con contraseña `123456` de la migración previa — el Paso 12 (invitaciones) las regulariza; mientras tanto, Mario puede pedir a los pacientes activos que la cambien. Siguiente: Paso 03 (Opus). |
| 2026-08-05 | Paso 01 (Fase A) | **Cerrada la recuperación insegura y el registro público.** `forgot-password` ya no devuelve el token de reseteo en la respuesta y responde idéntico exista o no el email (sin enumeración de cuentas). Eliminados `POST /auth/register`, `POST /auth/verify-identity` y `PUT /auth/set-password/:verifyToken` — el flujo de RUT + fecha de nacimiento permitía tomar control de cuentas ajenas. Se adelantó el arreglo de `notFound` (antes toda ruta inexistente respondía 500, lo que impedía verificar que los endpoints se hubieran ido; estaba programado para el Paso 07(d), ya marcado como resuelto ahí). Frontend: `RecoverPassword` pasa a versión interina con WhatsApp, y se eliminó `register` de `authService`/`AuthContext`. Verificado con servidor local contra Atlas: los 3 endpoints responden 404 y las respuestas de `forgot-password` son idénticas byte a byte. 771 líneas de código inseguro eliminadas. | Opus 5 | `ced8700` (tag `step-01-auth-doors`) | **Falta desplegar:** push a `main` (Railway) + subir build a Hostinger. Sin desplegar, la vulnerabilidad sigue viva en producción. Siguiente: Paso 02 (Opus). |
| 2026-08-05 | Blueprint | Análisis exhaustivo del código (backend + frontend) con metodología the-architect (brownfield) y generación de `BLUEPRINT.md`: 20 secciones, 28 pasos con verificación ejecutable. Se detectaron 12 hallazgos críticos (V1–V12), incl. token de reseteo devuelto en la respuesta, cuentas auto-creadas desde Airtable con contraseña `123456`, IDORs de fichas médicas, escalada de roles, `.env.production` en git (rotar credenciales) y descuento de sesiones nunca implementado. Entrevista con Mario: cerrar accesos antiguos, sesión 30d renovable, archivos en R2, email con Resend. Ventana de agendamiento confirmada en 4h (resuelve el pendiente del 17-jul). `CLAUDE.md`, `STATUS.md` y `PROMPTS.md` actualizados/regenerados para reflejar el blueprint como plan vigente. | Fable 5 | (pendiente) | Próxima sesión: `BLUEPRINT.md` §9 Paso 01 (Opus, ver `PROMPTS.md`). Fase A completa = urgente, deploy tras cada paso. |

